{
  "private": true,
  "scripts": {
    "build": "dotnet pack packages/hotreloadnuget --configuration Release --output build",
    "commitmsg": "cross-env commitlint -e $GIT_PARAMS"
  },
  "devDependencies": {
    "@commitlint/config-angular": "^6.1.0",
    "@semantic-release/exec": "^5.0.0",
    "commitlint": "^6.1.0",
    "cross-env": "^5.1.3",
    "husky": "^0.14.3",
    "semantic-release": "^17.3.2",
    "shelljs": "^0.8.4"
  }
}